#include<stdio.h>
int main()
{
    int n,num,flag=0;
    printf("enter any positive number");
    scanf("%d",&n);
    
    
    while(n>1)
    {
        
        if(n==num)
        {
            flag=1;
        }
        n--;
    }
    if(flag==0)
    printf("UNIQUE");
    else
    printf("DUPLICATE");
}
