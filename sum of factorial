#include<stdio.h>
int main()
{
    int n;
    int i=1;
    int sum=0;
    printf("enter n value\n");
    scanf("%d",&n);
    while(i<=n)
    {
        if(n%i==0)
        sum=sum+i;
        i++;
    }
    printf("sum of factors of given number=%d",sum);
}
